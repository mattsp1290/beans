## Action Items

### Critical
- [ ] [apps/bean-counter/scripts/deploy-production.sh:227-248] Replace `check_sanctioned_replace`'s `grep -F` substring matching with an exact, comment-stripped, whitespace-normalised equality test against `$SANCTIONED_BEANS_REPLACE`. Today `replace github.com/mattsp1290/beans/libs/beans => ../../libs/beans/vendored-fork` is **accepted** (verified rc=0); the old `grep -E 'replace.*github\.com/mattsp1290/beans'` gate rejected it, and because that path is inside the tree `Dockerfile:31` copies, it builds and ships. Full drop-in replacement, validated against all 13 cases, is in `01-critical-and-important.md` §C1.
- [ ] [.github/workflows/ci-libs-beans.yml:50-54] The pinned `golangci-lint@v2.1.6` builds as `go1.23.4` and refuses `libs/beans/.golangci.yml:4`'s `run.go: "1.25"` with `can't load config`, exit 3 — so `make ci` fails at `lint` on every run of this workflow. Fix by pinning v2.12.2 through `go run` in `libs/beans/Makefile:21-22` (mirroring `apps/bean-counter/Makefile:2-3`) and deleting the install step. Verified: v2.12.2 loads the config and reports `0 issues.`

### Important
- [ ] [apps/bean-counter/scripts/deploy-production.sh:231] The gate is satisfied by a *commented-out* replace — a `go.mod` containing only `// replace github.com/mattsp1290/beans/libs/beans => ../../libs/beans` passes (verified rc=0). Closed by the C1 fix's comment stripping.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:243] A real second replace whose trailing comment repeats the sanctioned text is filtered out by `grep -qvF` and never reported (verified rc=0). Closed by the C1 fix.
- [ ] [apps/bean-counter/test/scripts/deploy-production_test.sh:116-125] The `block.mod` case is vacuous: it is rejected by the "must contain" branch at script `:231` and never reaches the `replace (` branch at `:238`, which therefore has zero coverage. Add a `sanctioned line + separate replace block` case that actually reaches it, plus regression cases for the three bypasses above (snippets in `01-critical-and-important.md` §I3).
- [ ] [.github/workflows/ci-apps-bean-counter.yml:42-66] No CI job runs `apps/bean-counter`'s `tidy-check`, though `libs/beans` gets it via `make ci`. With `go.work` now present, a workspace-active `go mod tidy` can write a `go.sum` incomplete for the `GOWORK=off` container build — the exact hazard `CLAUDE.md` documents — and it would first surface mid-deploy. Add a `- name: Tidy check / run: make tidy-check` step between Test and Build.
- [ ] [.dockerignore + .github/workflows/ci-apps-bean-counter.yml:6-22] `/.dockerignore` is a load-bearing input to the root-context API build but appears in no workflow's `paths` filter, and no CI job builds either image. A PR editing only that file triggers `ci-workspace`, which never runs Docker. Add `.dockerignore` to both `paths` lists and add an image-build job running `docker build -f apps/bean-counter/Dockerfile -t bean-counter-api:ci .` and `docker build -t bean-counter-ui:ci ./apps/bean-counter/frontend`.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:361,:397] Both `go list -m` invocations in the deployability gate run with the workspace active, while the image resolves with `GOWORK=off`. Prefix both with `GOWORK=off` so the gate validates the resolution path that actually ships; the only local gate that currently exercises `GOWORK=off` is the Docker build at `:450`, which `--skip-local-build` removes.

### Suggestions
- [ ] [Makefile:5] Add `tidy-check` to `.PHONY` — the target at `:24` is declared but not marked phony.
- [ ] [Makefile:32-34] `ci-integration` uses `cd libs/beans && go test` for one module and `$(MAKE) -C` for the other. Add a `test-integration` target to `libs/beans/Makefile` and fan out uniformly with `-C`.
- [ ] [apps/bean-counter/deploy/README.md:4-5] Broken link: `../.agents/plans/deploy/` resolves to the nonexistent `apps/bean-counter/.agents/...`. Point it at `../../../.agents/plans/bean-counter-deploy/`.
- [ ] [apps/bean-counter/deploy/README.md:37 and apps/bean-counter/deploy/docker-compose.prod.yml:59] `deploy/k8s/bean-counter-ingress.yaml` is wrong from both the repo root and the file's own directory. Use `apps/bean-counter/deploy/k8s/bean-counter-ingress.yaml`.
- [ ] [.dockerignore:11-14] The comment claims a root-level `frontend` exclusion "would be wrong", but this file never applies to the UI build (its context is `apps/bean-counter/frontend`, which has its own `.dockerignore`). Either add `**/frontend` to shrink the API build context, or reword the comment.
- [ ] [.github/workflows/ci-libs-beans.yml:20-22] Move `defaults.run.working-directory` from workflow level into the `gates` job, matching `ci-apps-bean-counter.yml`; otherwise a future second job silently inherits `libs/beans` as its cwd.
- [ ] [.github/workflows/ci-workspace.yml:38-41] `git diff --exit-code` cannot see untracked files, so a `go.work.sum` newly emitted by `go work sync` would pass the gate (`.gitignore` no longer ignores it). Assert `test -z "$(git status --porcelain)"` instead.
- [ ] [.github/workflows/*.yml] Unify trigger filters: `ci-workspace` has no branch filter (double-runs every PR push), `ci-libs-beans` filters paths but not branches, `ci-apps-bean-counter` restricts push to `main`.
- [ ] [.github/workflows/ci-apps-bean-counter.yml + ci-libs-beans.yml] Confirm branch-protection required checks: a PR touching only root files matches neither path filter, so any filtered job configured as required would block such a PR forever.
- [ ] [libs/beans/.golangci.yml:23] Delete the empty `settings:` key under `linters:` — it parses as null and makes `golangci-lint config verify` fail (`got null, want object`), though `run` tolerates it.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:962-966] Add a working-directory guard at the top of `main()` (`cd "$(git rev-parse --show-toplevel)"`); running from `apps/bean-counter/` currently fails with the misleading "module graph is not deployable".
- [ ] [apps/bean-counter/test/scripts/deploy-production_test.sh:53,:75] `$tmp` is created 22 lines before the EXIT trap is installed. Create both temp dirs up front and install the trap immediately after.
- [ ] [libs/beans/Makefile:29 and apps/bean-counter/Makefile:40] `git diff --exit-code go.mod go.sum` compares against the index, so a staged untidy `go.mod` passes. Use `git diff --exit-code HEAD -- go.mod go.sum`.
- [ ] [.gitignore] The dropped `apps/bean-counter/.gitignore` carried `.agents/reviews/`; the root file has no equivalent, and `require_clean_local_ref` now aborts on any untracked file anywhere in the monorepo. On this machine that is masked by a *global* ignore (`~/.gitignore_global:1:reviews/`, confirmed via `git check-ignore -v`), so a fresh clone or a different operator would hit the abort. Re-add repo-level ignores for agent/review output, or document the sweep in `deploy/README.md`.
- [ ] [apps/bean-counter/README.md:17-70] Every command in this file assumes cwd `apps/bean-counter/`, while the root docs say "run from the repository root". State the working directory once near the top.
