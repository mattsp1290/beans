## Action Items

### Critical

- [ ] None. No security, data-loss, crash, or broken-functionality defect found in the migration-authored surface.

### Important

- [ ] [apps/bean-counter/deploy/README.md:61-65 and .agents/plans/monorepo-consolidation/05-containers-and-deploy.md:278-300] Document that un-pinning the library raises the embedded beans Postgres migration max from `0008` (pinned `v0.1.2-0.20260615002029-e52dce57b52c`) to `0011` (`libs/beans` HEAD), so the deploy script's mandatory schema-version parity gate (`apps/bean-counter/scripts/deploy-production.sh:684`) will abort the first post-merge deploy with "embedded migrations (11) NEWER than prod (8)". Add the required pre-deploy step: advance the shared local-symphony Postgres to `0011` out of band with `bn` against the shared DSN, verify `select max(version_id) from bn_schema_versions` returns 11, and review `0010_bn_issue_state_drop_check.sql` (drops the `bn_issues_state_check` CHECK) and `0011_bn_issue_repos_creation_commit.sql` (adds `creation_commit`) against the orchestrator's expectations first. File a bead for the database migration itself. The gate is working correctly — this is a missing operator step, not a code fix.

### Suggestions

- [ ] [apps/bean-counter/internal/handlers/deps/deps_test.go:49-51 and internal/handlers/graph/graph_test.go:50-55] Add `if store.listFilter.AllRepos { t.Fatal(...) }` (and the same for `depsFilter`). The fakes assert only on `Prefix`, but `AllRepos` is the field that would silently drop the prefix `WHERE` clause and return every project's rows.
- [ ] [apps/bean-counter/internal/handlers/graph/graph_test.go:76-78] Replace the `depsFilter.Prefix != ""` proxy for "not called" with an explicit `depsCalled bool` set inside the fake's `ListBlockingDeps`.
- [ ] [Makefile:5] Re-add `tidy-check` to `.PHONY` (it was there pre-rename and the target still exists at line 24), and either restore a root `install` target or document `make beans TARGET=install` in `AGENTS.md`.
- [ ] [Makefile:33-35] `ci-integration` inlines `cd libs/beans && go test -tags=integration ./...`, the only build logic in a file whose header says it has none. Add a `test-integration` target to `libs/beans/Makefile` and fan out with `$(MAKE) -C libs/beans test-integration`.
- [ ] [libs/beans/Makefile:7] `git describe --tags --match 'libs/beans/v*' --always --dirty` yields a bare hash (`40d8e64`) because no matching tag exists, so `bn --version` lost its semantic version (was `v0.1.1-305-g40d8e64`). Either cut a `libs/beans/v0.1.2` tag as part of the cutover or add a `--match 'v[0-9]*'` fallback; if a prefixed tag is cut, strip the `libs/beans/` prefix from the version string.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:392] Add a `require_repo_root` guard (`git rev-parse --show-toplevel` must equal `$PWD`) called before `resolve_target_sha`. Every local path is now repo-root-relative with nothing enforcing the cwd.
- [ ] [README.md:66] `[beads](https://github.com/mattsp1290/beans)` links the beads tracker tool at this repository. Point it at the real beads project or drop the link.
- [ ] [setup-beads.sh, setup-multi-repo-beads.sh] Both root-level scripts still name pre-monorepo paths (`cmd/bn/app.go`, `store/store.go`, `schema/`). Nothing breaks (the strings only reach `bd create` descriptions), but they sit outside the `.agents/` tree that `CLAUDE.md` declares frozen. Move them under `libs/beans/scripts/historical/` or add a header noting the paths predate the monorepo.
- [ ] [.github/workflows/ci-apps-bean-counter.yml:47-62, ci-libs-beans.yml:200-212] Record in `CLAUDE.md` that `ci-workspace` is the workflow to mark as a required status check — the other two are path-filtered and report nothing on a PR touching neither path. Also align the `push` triggers: `ci-apps-bean-counter` restricts to `branches: [main]`, `ci-libs-beans` does not.
- [ ] [.github/workflows/ci-workspace.yml:298-301] `go work sync` followed by `git diff --exit-code` cannot see a newly generated untracked `go.work.sum` (none exists today, and `.gitignore` no longer ignores it). Add `test -z "$(git status --porcelain)"` after the diff.
- [ ] [apps/bean-counter/Makefile:31-32] `fmt-check`'s `find . -not -path './.git/*'` exclusion is inert in a monorepo module while `find .` now descends into `frontend/node_modules`. Scope it to `gofmt -l ./cmd ./internal ./test`.
- [ ] [apps/bean-counter/internal/api/validate/validate.go:37-42] bean-counter's five-state `allowedStates` was left unreconciled with `libs/beans` HEAD's eight-status `model.DefaultWorkflowConfig()` (adds `ready_for_review`, `ready_for_validation`, `ready_for_merge`). Not a regression — bean-counter was already the stricter layer and `/ready` semantics are preserved — but an issue the `bn` CLI puts into a `ready_for_*` state in the shared database cannot be moved out of it through the API. Either wire `allowedStates` to `store.WorkflowConfig()` (exposed at `libs/beans/store/store.go:82-85`) or file a bead recording the deliberate divergence.
