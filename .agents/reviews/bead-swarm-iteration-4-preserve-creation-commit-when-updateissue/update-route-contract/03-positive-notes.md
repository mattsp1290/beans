# Positive Notes

- Validation happens before the old repo link is removed, avoiding accidental clearing on bad explicit values.
- Existing immutable metadata wins over explicit replacement values when a repo link already exists.
- The tests cover both retargeting and route-only update behavior.
