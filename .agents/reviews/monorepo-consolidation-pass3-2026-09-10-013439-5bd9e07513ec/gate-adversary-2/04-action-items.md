## Action Items

### Critical
- [ ] [apps/bean-counter/scripts/deploy-production.sh:431] Containment-check the directory that is actually read. `beans_phys` is resolved and contained, then three unresolved components (`schema/migrations/postgres`) are appended; a symlink at any of them puts `EMBEDDED_MAX` back outside the repo with all gates green (measured 1 instead of 7). Resolve `"$beans_phys/schema/migrations/postgres"` with `cd … && pwd -P` and run the same `case` against that, or reject symlinks under `libs/beans` outright.

### Important
- [ ] [apps/bean-counter/scripts/deploy-production.sh:458] Widen the structural check beyond `libs/beans`'s final component. `apps/bean-counter` and `apps/bean-counter/go.mod` — the file the replace gate reads — can both be out-of-tree symlinks with every gate green and the worktree clean. Replace `[ ! -L libs/beans ]` with a tree-level `find libs/beans apps/bean-counter -type l -print -quit` that must be empty; this also subsumes the Critical above and turns the accidental `libs`-as-symlink catch into a designed one with an accurate message.
- [ ] [apps/bean-counter/test/scripts/deploy-production_test.sh:160] Add regression tests for the three pass-2 fixes that currently have none. Deleting the `libs/beans` symlink refusal, deleting the containment `case`, or reverting `require_repo_root` to logical `$PWD` each leaves the suite at 42/42. Build a throwaway repo in the harness and assert: symlinked `libs/beans` rejected; repo reached through a symlinked parent accepted; `libs/beans` resolving outside the root rejected; `EMBEDDED_MAX` correct (or the call aborting) when `libs/beans/schema` is an out-of-tree symlink.

### Suggestions
- [ ] [apps/bean-counter/scripts/deploy-production.sh:286] Use `[ "$count" != "1" ]` instead of `-gt 1`. `set -e` is suspended inside this function (the sole caller is `… || fatal`), so a non-numeric `count` makes `[` return 2, the `if` false, and control falls through to the single-directive comparison, which accepts whenever the first directive is the sanctioned one.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:236] Make the parser independently sound. A `replace` line prefixed by a UTF-8 BOM or NBSP is invisible to `^[[:space:]]*replace` and the gate accepts the file; only `go`'s own rejection stops it today, and that runs later. Either treat an unclassifiable `replace` token as a parse failure, or derive the directive list from `GOWORK=off go mod edit -json` and keep the awk parser as a second opinion that must agree.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:230] Soften the comment about the command-substitution status capture. With `LC_ALL=C` in place I could not construct any input where reverting it (M3) or dropping the `|| { … return 1; }` handler (M4) changes the verdict — including unreadable files, directories and NUL-bearing files, which fail closed either way. Keep the code, describe it as defence in depth rather than a demonstrated behaviour.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:1049] Move `resolve_embedded_migration_max` after `require_clean_local_ref` (or hoist `check_sanctioned_replace` into `main`). Today `go list -m` runs against an unvetted `go.mod` and `--dry-run` prints `embedded_max` with no content gate having run.
- [ ] [apps/bean-counter/scripts/deploy-production.sh:186] Consider deriving the parity number from the library's `embed.FS` rather than the on-disk `*.sql` filenames, so the local number and the number compiled into the image are the same object. A symlinked `0007_x.sql` inside an otherwise contained directory still drops the max from 7 to 3.
