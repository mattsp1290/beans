# Suggestions

- `README.md` and `docs/bn.toml.example` still describe the prior "walking up from the working directory" config discovery behavior. Updating docs with the `.bn` marker step and `BN_STATUS_DEFAULT` would help users, but documentation wiring was outside this loader-focused bead.

