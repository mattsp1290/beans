# Positive Notes

- `README.md` explains the common operational paths without overloading users with implementation detail.
- `docs/specs/repo-resolution-precedence.md` covers auto-detect, `.bn` marker, explicit `--repo`, local-only repos, mismatch cases, and best-effort failures.
- `docs/specs/topology-a-prefix-equals-slug.md` correctly keeps `creation_commit` out of repo registry semantics.
