# Status: IMPLEMENTED & MERGED (2026-06-09)

The postgres tracker adapter (`internal/tracker/postgres`) and the `bn` CLI (`cmd/bn`) are merged on `main`; runtime dispatches `tracker.kind: postgres`. Postgres is now the chosen source of truth, pending end-to-end vetting. This plan dir is the design record.

