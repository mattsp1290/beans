# Bean Counter Frontend

Svelte + Vite frontend for the Bean Counter API.

## Development

```sh
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:8080` by default. Set
`VITE_API_PROXY_TARGET` to point at a different API address.

## Checks

```sh
npm run check
npm run build
```
